# ── STRIPE ──
STRIPE_SECRET_KEY=sk_live_REPLACE_ME
STRIPE_PUBLIC_KEY=pk_live_REPLACE_ME       # already set in js/config.js

# ── HOSTINGER MYSQL ──
# Find these in: hPanel → Databases → MySQL Databases
DB_HOST=localhost                           # always localhost on Hostinger
DB_USER=REPLACE_ME                         # e.g. u123456789_btm
DB_PASSWORD=REPLACE_ME
DB_NAME=REPLACE_ME                         # e.g. u123456789_btm

# ── TWILIO ──
TWILIO_ACCOUNT_SID=ACREPLACE_ME
TWILIO_AUTH_TOKEN=REPLACE_ME
TWILIO_FROM_NUMBER=+REPLACE_ME

# ── STAFF PHONES (SMS alerts on new booking) ──
STAFF_PHONE_SALDANHA=+351925693355
STAFF_PHONE_CAPARICA=+351932141557

# ── RESEND (email confirmations) ──
RESEND_API_KEY=re_REPLACE_ME

# ── MB WAY / SIBS (optional) ──
MBWAY_API_KEY=REPLACE_ME
MBWAY_MERCHANT_ID=REPLACE_ME

# ── SERVER ──
PORT=3000
FRONTEND_URL=https://bangkokthaimassage.pt
