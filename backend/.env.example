# ── STRIPE ──
STRIPE_SECRET_KEY=sk_live_REPLACE_ME
STRIPE_PUBLIC_KEY=pk_live_REPLACE_ME   # used in frontend config.js

# ── SUPABASE ──
SUPABASE_URL=https://REPLACE.supabase.co
SUPABASE_SERVICE_ROLE_KEY=REPLACE_ME   # service_role key (NOT anon key)

# ── TWILIO ──
TWILIO_ACCOUNT_SID=ACREPLACE_ME
TWILIO_AUTH_TOKEN=REPLACE_ME
TWILIO_FROM_NUMBER=+REPLACE_ME         # your Twilio phone number

# ── STAFF PHONES (for new booking alerts) ──
STAFF_PHONE_SALDANHA=+351925693355
STAFF_PHONE_CAPARICA=+351932141557

# ── RESEND (email) ──
RESEND_API_KEY=re_REPLACE_ME

# ── MB WAY / SIBS ──
MBWAY_API_KEY=REPLACE_ME
MBWAY_MERCHANT_ID=REPLACE_ME

# ── SERVER ──
PORT=3000
FRONTEND_URL=https://bangkokthaimassage.pt   # set to your domain, or * for dev
